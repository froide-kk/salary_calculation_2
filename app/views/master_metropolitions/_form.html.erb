<%= form_with(model: master_metropolition, local: true) do |form| %>
  <% if master_metropolition.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(master_metropolition.errors.count, "error") %> prohibited this master_metropolition from being saved:</h2>

      <ul>
      <% master_metropolition.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :index %>
    <%= form.number_field :index, id: :master_metropolition_index %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :master_metropolition_name %>
  </div>

  <div class="field">
    <%= form.label :allowance %>
    <%= form.number_field :allowance, id: :master_metropolition_allowance %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
