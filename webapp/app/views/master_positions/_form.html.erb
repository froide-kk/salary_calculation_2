<%= form_with(model: master_position, local: true) do |form| %>
  <% if master_position.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(master_position.errors.count, "error") %> prohibited this master_position from being saved:</h2>

      <ul>
      <% master_position.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <table class ="seting">
      <tr>
        <td><%= form.label :name %></td>
        <td><%= form.text_field :name, id: :master_position_name %></td>
      </tr>

     <tr>
       <td><%= form.label :index %></td>
       <td><%= form.number_field :index, id: :master_position_index %></td>
     </tr>

     <tr>
       <td><%= form.label :functional_allowance %></td>
       <td><%= form.number_field :functional_allowance, id: :master_position_functional_allowance %></td>
     </tr>

     <tr>
       <td><%= form.label :duty_allowance %></td>
       <td><%= form.number_field :duty_allowance, id: :master_position_duty_allowance %></td>
    </tr>

    <tr>
      <td><%= form.label :deemded_overtime %></td>
      <td><%= form.number_field :deemded_overtime, id: :master_position_deemded_overtime %></td>
    </tr>

    <tr>
      <td><%= form.label :standard_age %></td>
      <td><%= form.number_field :standard_age, id: :master_position_standard_age %></td>
    </tr>
  </table>
</div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
