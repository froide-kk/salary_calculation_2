<%= form_with(model: master_job, local: true) do |form| %>
  <% if master_job.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(master_job.errors.count, "error") %> prohibited this master_job from being saved:</h2>

      <ul>
      <% master_job.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :index %>
    <%= form.number_field :index, id: :master_job_index %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :master_job_name %>
  </div>

  <div class="field">
    <%= form.label :allowance %>
    <%= form.number_field :allowance, id: :master_job_allowance %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
