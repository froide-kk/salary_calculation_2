<%= form_with(model: company, local: true) do |form| %>
  <% if company.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(company.errors.count, "error") %> prohibited this company from being saved:</h2>

      <ul>
      <% company.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :this_year %>
    <%= form.number_field :this_year, id: :company_this_year %>
  </div>

  <div class="field">
    <%= form.label :last_year %>
    <%= form.number_field :last_year, id: :company_last_year %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
